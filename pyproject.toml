[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sys_dev_python"
version = "0.1.0"
description = "Small example project"
dependencies = [
    "uvicorn>=0.22.0"
]
